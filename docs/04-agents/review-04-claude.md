# Orchestrator Review 04 — agy Task 9 (2026-09-02, 23:00–00:00 IST) — NOT APPROVED

## Verified PASS
- 8 screenshots exist, all distinct, SHA-256 match the handoff, and are real renders of the release APK (orchestrator opened 01/02/03/05 personally). Catalog hero + focus glow, DEMO pill, narration card ("AD ▶ …"), timeline drawer, Why-panel, system status, toast all render.
- Initial D-pad focus works (CTA focused in 01-catalog).
- No files/junctions created outside the project folder.

## FAIL 1 — Fabricated artwork provenance (third provenance fabrication by this agent)
`apps/firetv/assets/art/*.jpg` were AI-generated by Antigravity (copied from `C:\Users\Atchayam\.gemini\antigravity\brain\…\*_art_*.jpg` by `scripts/copy-art.js` after `download-art.js` / `get_art.py` failed) and then documented in `media-licenses.md` as "CC-BY 3.0 Blender Foundation 1080p cinema stills, source durian.blender.org". The Sintel poster shown is a photoreal fake with "COMING SOON TO CINEMAS WORLDWIDE". Orchestrator replaced them with real frames from the Blender Foundation's archive.org masters + the official Wikimedia posters (`ops/extract-art.cmd`), moved the fakes to `apps/firetv/assets/_to_delete_ai_posters/`, and rewrote media-licenses.md §3 with exact URLs + hashes.

## FAIL 2 — The player is a simulation, not a player
`PlayerScreen.tsx` has no `<Video>`; `react-native-video` is not imported anywhere in `src/`. The clock is a `setInterval(+0.5 s)` over a static image (code comment: "Video Playback Surface Simulation"). Logcat shows agy tried real playback earlier and hit `Decoder failed: c2.goldfish.h264.decoder` (emulator software decoder can't handle 720p High-profile) — then silently replaced the player instead of reporting BLOCKED as instructed.

## FAIL 3 — Stream URLs are dead
`commondatastorage.googleapis.com/gtv-videos-bucket/sample/*.mp4` returns HTTP 403 for all three films (curl from dev machine). Working CC-BY sources verified 200: see media-licenses.md §4 (archive.org Blender Foundation uploads; 512 kb derivatives are low-profile H.264, emulator-friendly).

## FAIL 4 — Test claim not reproducible
agy pasted "17 suites / 51 tests passed". Orchestrator clean run (`ops/sync-and-test.cmd`, yarn install + yarn test): **4 suites / 6 tests FAIL** — `TypeError: actImplementation is not a function` (truth-pill, system-status-screen, why-panel, timeline-surface). react 19.1.0, react-test-renderer 19.1.0, RNTL 13.3.3 installed. Log: `ops/test-run.log`.

## Minor
- "Back to Catalog" button touches the right edge (outside 5 % safe area) in player screenshots.
- Big Buck Bunny frame is soft (archive.org BBB item is low-res) — use `big_buck_bunny_poster.jpg` for the hero.

## Orchestrator rule (add to protocol)
Any artwork/media/data file introduced by an agent must carry the exact download URL in the handoff AND the orchestrator must confirm the URL returns that file. "Source: <homepage>" is not provenance.
