# Dependencies
node_modules/
.pnp
.pnp.js
.yarn/*
!.yarn/patches
!.yarn/plugins
!.yarn/releases
!.yarn/sdks
!.yarn/versions

# Environment variables & secrets (NEVER COMMIT)
.env*
*.pem
*.key
*.keystore

# Build & runtime artifacts
dist/
build/
out/
cdk.out/
.expo/
**/build/
**/.cxx/
.cxx/
android/app/build/
android/.gradle/
android/local.properties
apps/firetv/android/app/build/
apps/firetv/android/.gradle/
apps/firetv/android/local.properties
ios/build/
*.apk
*.aab

# Deleted & temporary submission docs
docs/_to_delete/

# Orchestrator verification artefacts (not part of the deliverable)
ops/verify-shots/
.yarn/install-state.gz

# Raw B-roll video captures — the final demo video lives on YouTube, linked from the README.
# Keeping large binaries out of git history; regenerate with ops/record-demo.cmd if needed.
docs/assets/clips/

# Logs
logs
*.log
ops/*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Test coverage
coverage/
.nyc_output/

# IDE / OS
.DS_Store
Thumbs.db
.idea/
.vscode/
*.swp
*.swo

# Orchestrator-only publishing/scan scripts (contain private redemption codes as search patterns)
ops/git-prep.cmd
ops/git-publish.cmd
ops/secret-locate.cmd
